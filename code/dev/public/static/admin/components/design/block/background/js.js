!function ($, ss) {
    "use strict";

    var name = "adminComponentsDesignBlockBackground";

    var parameters = {
        /**
         * Parent
         *
         * @var {String}
         */
        parent: "",

        /**
         * Init
         */
        init: function () {

        }
    };

    ss.add(name, parameters);
}(window.jQuery, window.ss);
