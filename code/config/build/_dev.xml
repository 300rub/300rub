<?xml version="1.0" encoding="UTF-8"?>
<project name="dev">
    <target name="dev-upload-prepare">
        <delete dir="${basedir}/../upload/dev" />
        <mkdir dir="${basedir}/../upload/dev" />
        <exec executable="chmod" failonerror="true">
            <arg line="-R 0777 ${basedir}/../upload/dev" />
        </exec>
    </target>

    <target
        name="dev-setup"
        depends="
            dev-upload-prepare,
            tools-flush-memcached,
            db-recreate-system-databases,
            db-generate-source,
            db-recreate-dev-database
        "
    >
    </target>
</project>
