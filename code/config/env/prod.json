{
  "host": "supers.site",
  "db": {
    "root": {
      "{{dbHost}}": {
        "user": "{{dbRootUser}}",
        "password": "{{dbRootPassword}}"
      }
    },
    "dev": {
      "host": "{{dbHost}}",
      "user": "{{dbDevUser}}",
      "password": "{{dbDevPassword}}",
      "name": "{{dbDevName}}"
    },
    "phpunit": {
      "host": "{{dbHost}}",
      "user": "{{dbPhpunitUser}}",
      "password": "{{dbPhpunitPassword}}",
      "name": "{{dbPhpunitName}}"
    },
    "selenium": {
      "host": "{{dbHost}}",
      "user": "{{dbSeleniumUser}}",
      "password": "{{dbSeleniumPassword}}",
      "name": "{{dbSeleniumName}}"
    },
    "system": {
      "host": "{{dbHost}}",
      "user": "{{dbSystemUser}}",
      "password": "{{dbSystemPassword}}",
      "name": "{{dbSystemName}}"
    },
    "help": {
      "host": "{{dbHost}}",
      "user": "{{dbHelpUser}}",
      "password": "{{dbHelpPassword}}",
      "name": "{{dbHelpName}}"
    },
    "source": {
      "host": "{{dbHost}}",
      "user": "{{dbSourceUser}}",
      "password": "{{dbSourcePassword}}",
      "name": "{{dbSourceName}}"
    }
  },
  "memcached": {
    "host": "{{memcachedHost}}",
    "port": "{{memcachedPort}}",
    "expiration": 1500
  },
  "file": {
    "urlMask": "%s://front.supers.site/%s/%s"
  },
  "aws": {
    "client": {
      "region": "eu-central-1",
      "version": "latest"
    },
    "s3": {
      "buckets": {
        "main": "{{s3BucketMain}}",
        "release": "{{s3BucketRelease}}"
      }
    }
  },
  "email": {
    "host": "smtp.gmail.com",
    "username": "ss.test.dev.local",
    "password": "mypasS77",
    "smtpSecure": "587",
    "port": "465",
    "fromAddress": "ss.test.dev.local@gmail.com",
    "fromName": "SS test dev"
  },
  "superPassword": "mypasS77"
}
