#!/bin/bash

path=${1/$PWD\//}

if [[ $path == "" ]]; then
  docker exec -it tests_phpfpm_1 ant lesshint -Dpath="/var/www/public/less"
  exit 1
fi

docker exec -it tests_phpfpm_1 ant lesshint -Dpath="/var/www/$path"