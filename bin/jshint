#!/bin/bash

path=${1/$PWD\//}

if [[ $path == "" ]]; then
  docker exec -it tests_phpfpm_1 ant -file config/build/build.xml jshint -Dpath="/var/www/public/js"
  exit 1
fi

docker exec -it tests_phpfpm_1 ant -file config/build/build.xml jshint -Dpath="/var/www/$path"