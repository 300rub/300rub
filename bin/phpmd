#!/bin/bash

path=${1/$PWD\//}

if [[ $path == "" ]]; then
  docker exec -it ss_phpfpm_1 ant -file config/build/build.xml phpmd -Dpath=.
  exit 1
fi

docker exec -it ss_phpfpm_1 ant -file config/build/build.xml phpmd -Dpath=$path